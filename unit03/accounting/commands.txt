**initdb.py:
建立一個連線到MongoDB資料庫的Python程式，
連線字串conn="mongodb://admin:mymdb$1234@localhost:27017"，
連線之後，利用try/except機制建立unit03db資料庫，
並在其中建立user以及transaction兩個Collection。
並新增一個使用者，內容是{"username":"minhuang", "password":"1234"}。

**main.py:
請幫我建立一個Python程式(main.py)，使用Tkinter模組建立視窗，一開始先顯示使用者登入帳號及密碼的功能。
此程式要連線到PyMongo，conn="mongodb://admin:mymdb$1234@localhost:27017"，
使用資料庫是unit03db，user集合，
用於密碼驗證之用，user集合中分別使用username記錄使用者名稱，以及password記錄使用者密碼。
視窗長寬800x500，白底，標題是藍底白字，標題文字為"個人記帳程式"，標題字位於程式上方放大並置中。
請修改main.py，使其在輸入正確的帳號及密碼之後，把畫面改為垂直排列的按鈕，
分別是新增收入、新增支出、瀏覽收支、用戶資訊、登出。按鈕字體為微軟正黑體、粗體字。
登出之後，請回到可以登入的畫面


**新增收入：
接著，請建立一個"新增收入"的表單，在按下"新增收入"按鈕之後即可啟用。
可輸入項目包括"日期"、"摘要"、"金額"、"備註"。其中日期要使用日期選擇器。
底下的按鈕包括"新增"、"新增並返回"、"清除"及"直接返回"等4個按鈕。
上方標題改為"新增收入"
"新增"按鈕會檢查所有內容，除了備註之外，其它的欄位都要有內容才能夠新增到資料集合中。
如果有缺少資料，需要以訊息提要重新輸入。
如果資料備齊，則新增到集合transaction中，
transaction集合的欄位分別是user、date、amount、category、memo。
其中category的內容固定為"income"。
"新增"之後，隨即清除欄位，讓使用者可以再次輸入。
如果是"新增並返回"，則在輸入完畢之後，隨即返回主畫面。
如為"清除"，則直接清除目前的輸入內容，"返回"按鈕則直接返回主畫面。

**新增支出：
請用和新增收入同樣的方式，加上新增支出的功能。
在transaction集合中，category欄位固定為expense。
上方標題改為"新增支出"

**瀏覽收支：
接著，請加上瀏覽收支的功能。
瀏覽收支的畫面要加上月份的選擇以及收入、支出、全部顯示等選擇的篩選功能。
收入、支出、全部顯示請使用一個下拉式選單，月份選擇請使用年和月的下拉式組合選單。
上方標題改為"瀏覽收支"字樣。

選擇完畢之後，請單獨條列出每一筆項目的摘要、日期（不需要年和月）及金額。
在顯示項目的最上方，需要有一個醒目的合計金額。支出使用負數，收入使用正數。
如為負數則使用紅色字體，如為正數請使用綠色字體。

利用偵測滑鼠點擊單元格的方式，在點擊每一個項目的單元格時，出現視窗以詢問是否刪除該記錄。
此外，請為此畫面加上垂直捲軸，以使所有的項目都能夠被檢視。

**用戶資訊
接著請修改功能，上方用大字置中的方式顯示目前使用者名稱。
標題文字是"用戶資訊"，置中的藍底白字。
標題文字下方放置一個"返回主畫面"按鈕。
返回主畫面下方放置一個年份的下拉式選單，選單內容為最近5年。

年份下拉式選單的下方分成左右並排的兩個區塊。
左側區塊以小計的方式，以列表的方式呈現該使用者本年度每一個月份的收支結餘數字。第一欄是月份，第二欄是結餘（收支加總之後的結果）
右側區塊利用matplotlib繪製成直條圖。資料來源為左側本年度每月份的結餘數字。如結餘為正，使用綠色條向上畫，結餘為負，使用紅色條向下畫。0的位置在中間畫一條水平線。



